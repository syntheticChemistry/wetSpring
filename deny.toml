# SPDX-License-Identifier: AGPL-3.0-or-later
# cargo-deny configuration for wetSpring workspace.
#
# Run:     cargo deny check
# Install: cargo install cargo-deny

[graph]
targets = []
all-features = true

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
unmaintained = "workspace"
yanked = "deny"
ignore = []

[licenses]
unlicensed = "deny"
confidence-threshold = 0.8
allow = [
    "AGPL-3.0",
    "AGPL-3.0-only",
    "AGPL-3.0-or-later",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "CC0-1.0",
    "ISC",
    "MIT",
    "MPL-2.0",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
]

[[licenses.clarify]]
crate = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [{ path = "LICENSE", hash = 0xbd0e_ed23 }]

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
allow = []
deny = [
    # ecoBin compliance: zero C dependencies in application code.
    # These crates pull in C/C++ compilers or link native libraries.
    { crate = "openssl-sys", reason = "C dependency — use rustls" },
    { crate = "openssl", reason = "C dependency — use rustls" },
    { crate = "libz-sys", reason = "C dependency — use flate2 rust_backend" },
    { crate = "bzip2-sys", reason = "C dependency" },
    { crate = "lzma-sys", reason = "C dependency" },
    { crate = "zstd-sys", reason = "C dependency — use ruzstd" },
    { crate = "curl-sys", reason = "C dependency — use ureq/reqwest" },
    { crate = "libgit2-sys", reason = "C dependency — use gitoxide" },
    { crate = "libssh2-sys", reason = "C dependency" },
    { crate = "cmake", reason = "C build tool" },
    { crate = "cc", wrappers = ["flate2"], reason = "C compiler — allowed only via flate2 rust_backend" },
    { crate = "libffi-sys", reason = "C dependency" },
    { crate = "bindgen", reason = "C header parser" },
    { crate = "pkg-config", reason = "C build tool" },
    # Known transitive C deps:
    # - renderdoc-sys: via wgpu-hal (GPU debug tooling, not application code)
    # - libfuzzer-sys: in barracuda/fuzz/ (excluded from workspace, tooling only)
    # Neither is banned because they are infrastructure, not application code.
]
skip = []
skip-tree = []
workspace-default-features = "allow"
external-default-features = "allow"
allow-wildcard-paths = true

[sources]
unknown-registry = "deny"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
