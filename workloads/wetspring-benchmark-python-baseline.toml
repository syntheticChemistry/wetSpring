# wetSpring Python benchmark baseline — timing reference for Python vs Rust
# Generates performance comparison data across multiple domains

[metadata]
name = "wetspring-benchmark-python-baseline"
description = "Python performance baseline for cross-domain Rust speedup comparison"
version = "0.1.0"

[execution]
type = "native"
command = "$GATE_HOME/miniforge3/envs/bioinfo/bin/python3"
args = ["$SPRINGS_ROOT/wetSpring/scripts/benchmark_python_baseline.py", "--format", "json"]
working_dir = "$SPRINGS_ROOT/wetSpring"

[output]
format = "json"
schema = "toadstool-validate-v1"

[resources]
max_memory_bytes = 4294967296
max_cpu_percent = 80.0

[security]
isolation_level = "None"
