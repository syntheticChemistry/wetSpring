# wetSpring Exp001 Python baseline — Galaxy + QIIME2 reference pipeline
# Full 16S pipeline bootstrap validation against published Galaxy workflow

[metadata]
name = "wetspring-exp001-python-baseline"
description = "Exp001 Galaxy/QIIME2 bootstrap validation — ground truth 16S pipeline"
version = "0.1.0"

[execution]
type = "native"
command = "$GATE_HOME/miniforge3/envs/bioinfo/bin/python3"
args = ["$SPRINGS_ROOT/wetSpring/scripts/validate_exp001.py", "--format", "json"]
working_dir = "$SPRINGS_ROOT/wetSpring"

[output]
format = "json"
schema = "toadstool-validate-v1"

[resources]
max_memory_bytes = 2147483648
max_cpu_percent = 50.0

[security]
isolation_level = "None"
