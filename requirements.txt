# wetSpring Python dependencies — runtime tools and experiment environment.
#
# WARNING: This file uses minimum-version ranges for broad compatibility.
# It is NOT suitable for reproducing validation baselines. For baseline
# reproduction, use scripts/requirements.txt which pins exact versions.
#
# Install: pip install -r requirements.txt
#
# These are needed for:
# - Galaxy control experiments (Phase 1)
# - Track 2 validation scripts (asari, FindPFAS)
# - Diversity baseline (Python benchmark)

# Core scientific stack
numpy>=1.24
scipy>=1.12

# Track 2: LC-MS metabolomics
asari-metabolomics>=1.13

# Track 2: PFAS screening
pyopenms>=3.0

# Diversity baseline (Python benchmark)
scikit-bio>=0.6

# Galaxy tool management
ephemeris>=0.10
bioblend>=1.0
